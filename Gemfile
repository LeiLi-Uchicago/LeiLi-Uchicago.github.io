# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Gems that used to ship with Ruby but were removed from the default set in
# Ruby 3.4/4.0. Old Jekyll 3.x and its deps still expect them.
gem "base64"
gem "csv"
gem "bigdecimal"

